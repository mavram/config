set -g prefix C-a
set -g mouse on
set -g base-index 1
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Let nvim control cursor shape per vim mode (block → vertical line)
set -g allow-passthrough on
